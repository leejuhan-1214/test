{
  "name": "ishs-arena",
  "version": "3.1.0",
  "private": true,
  "description": "ISHS ARENA 계정 및 프라이빗 룸 멀티플레이 서버",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check battle-engine.js && node --check multiplayer.js && node --check app-v2.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
